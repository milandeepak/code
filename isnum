#include<stdio.h>
#include<string.h>

int main(){
str a[50];
int b[50],i,j=0;

printf("Enter string:");
gets(a);

 for(i=0;i<strlen(a);i++){
  if (isdigit(str[i]) != 0){
     b[j]==a[i];
     j++;
  }
  
 }
 for(i=0;i<strlen(b);i++)
 printf("%s",b[i]);
 
}
